---
category: patterns
topic: i18n
status: draft
---

## Проблема / Контекст

Интернационализация в Next.js 15 App Router с `next-intl` требует настройки: middleware для определения локали, `[locale]` route group, type-safe переводов, поддержки Server Components и Client Components, RTL-языков и SEO (hreflang). Встроенный `next/link` и `next/navigation` нужно заменить на версии из `next-intl/navigation`.

Ключевые задачи:
- Автоопределение локали (Accept-Language header)
- URL-based локали: `/en/dashboard`, `/ru/dashboard`
- Type-safe переводы — ошибка компиляции если ключ не существует
- Server Components используют `getTranslations()`, Client — `useTranslations()`
- SEO: `<link rel="alternate" hreflang="...">` в head
- RTL поддержка для Arabic, Hebrew и подобных

## Решение

**Установка:**
```bash
npm install next-intl
```

**Структура:** Middleware определяет локаль и редиректит. Все маршруты обёрнуты в `[locale]` segment. `NextIntlClientProvider` в layout провайдит переводы клиентским компонентам. Type-safe переводы через TypeScript path типы.

## Пример кода

### 1. Конфигурация локалей

```typescript
// src/i18n/config.ts
export const locales = ["en", "ru", "de", "ar"] as const;
export type Locale = (typeof locales)[number];

export const defaultLocale: Locale = "en";

// RTL языки
export const rtlLocales: Locale[] = ["ar"];

export function isRtl(locale: Locale): boolean {
  return rtlLocales.includes(locale);
}

// Метаданные локалей для switcher
export const localeNames: Record<Locale, string> = {
  en: "English",
  ru: "Русский",
  de: "Deutsch",
  ar: "العربية",
};
```

### 2. Файлы переводов

```json
// src/i18n/messages/en.json
{
  "common": {
    "loading": "Loading...",
    "error": "Something went wrong",
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "confirm": "Are you sure?",
    "pagination": {
      "previous": "Previous",
      "next": "Next",
      "of": "{current} of {total}"
    }
  },
  "auth": {
    "signIn": "Sign In",
    "signOut": "Sign Out",
    "signUp": "Create Account",
    "email": "Email address",
    "password": "Password",
    "forgotPassword": "Forgot password?",
    "errors": {
      "invalidCredentials": "Invalid email or password",
      "emailRequired": "Email is required",
      "passwordTooShort": "Password must be at least {min} characters"
    }
  },
  "dashboard": {
    "title": "Dashboard",
    "welcome": "Welcome back, {name}!",
    "stats": {
      "projects": "{count, plural, =0 {No projects} =1 {1 project} other {# projects}}",
      "members": "{count, plural, =0 {No members} =1 {1 member} other {# members}}"
    }
  },
  "metadata": {
    "home": {
      "title": "MyApp — Your productivity tool",
      "description": "Manage your projects and team efficiently"
    },
    "dashboard": {
      "title": "Dashboard | MyApp",
      "description": "View your projects and recent activity"
    }
  }
}
```

```json
// src/i18n/messages/ru.json
{
  "common": {
    "loading": "Загрузка...",
    "error": "Что-то пошло не так",
    "save": "Сохранить",
    "cancel": "Отмена",
    "delete": "Удалить",
    "confirm": "Вы уверены?",
    "pagination": {
      "previous": "Назад",
      "next": "Вперёд",
      "of": "{current} из {total}"
    }
  },
  "auth": {
    "signIn": "Войти",
    "signOut": "Выйти",
    "signUp": "Создать аккаунт",
    "email": "Email адрес",
    "password": "Пароль",
    "forgotPassword": "Забыли пароль?",
    "errors": {
      "invalidCredentials": "Неверный email или пароль",
      "emailRequired": "Email обязателен",
      "passwordTooShort": "Пароль должен быть не менее {min} символов"
    }
  },
  "dashboard": {
    "title": "Дашборд",
    "welcome": "С возвращением, {name}!",
    "stats": {
      "projects": "{count, plural, =0 {Нет проектов} =1 {1 проект} few {# проекта} other {# проектов}}",
      "members": "{count, plural, =0 {Нет участников} =1 {1 участник} few {# участника} other {# участников}}"
    }
  },
  "metadata": {
    "home": {
      "title": "MyApp — Ваш инструмент продуктивности",
      "description": "Управляйте проектами и командой эффективно"
    },
    "dashboard": {
      "title": "Дашборд | MyApp",
      "description": "Просматривайте проекты и последние действия"
    }
  }
}
```

### 3. Type-safe переводы через TypeScript

```typescript
// src/i18n/types.ts
// Автоматически генерирует типы из JSON файла
import type en from "./messages/en.json";

// Типизация для useTranslations
export type Messages = typeof en;

// Объявляем глобально для next-intl
declare module "next-intl" {
  interface AppConfig {
    Messages: Messages;
  }
}
```

### 4. next-intl конфиг

```typescript
// src/i18n/request.ts
import { getRequestConfig } from "next-intl/server";
import { notFound } from "next/navigation";
import { locales, type Locale } from "./config";

export default getRequestConfig(async ({ requestLocale }) => {
  const locale = await requestLocale;

  // Валидируем локаль
  if (!locale || !locales.includes(locale as Locale)) {
    notFound();
  }

  return {
    locale,
    messages: (await import(`./messages/${locale}.json`)).default,
    // Формат дат/чисел для локали
    formats: {
      dateTime: {
        short: {
          day: "numeric",
          month: "short",
          year: "numeric",
        },
        long: {
          day: "numeric",
          month: "long",
          year: "numeric",
          hour: "numeric",
          minute: "numeric",
        },
      },
      number: {
        currency: {
          style: "currency",
          currency: locale === "ru" ? "RUB" : "USD",
        },
      },
    },
  };
});
```

### 5. Middleware для определения локали

```typescript
// src/middleware.ts
import createMiddleware from "next-intl/middleware";
import { locales, defaultLocale } from "@/i18n/config";
import { auth } from "@/lib/auth";
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

// next-intl middleware обрабатывает locale routing
const intlMiddleware = createMiddleware({
  locales,
  defaultLocale,
  // Префикс всегда: /en/..., /ru/..., /de/...
  localePrefix: "always",
  // Определяем локаль из Accept-Language, cookie, subdomain
  localeDetection: true,
});

// Комбинируем с Auth.js middleware
export default auth(async (req) => {
  // Сначала обрабатываем интернационализацию
  const intlResponse = intlMiddleware(req as NextRequest);

  // Если интл делает редирект — пропускаем
  if (intlResponse.status !== 200) {
    return intlResponse;
  }

  // Дальше — твоя auth логика
  const { pathname } = req.nextUrl;
  const isProtected = pathname.match(/^\/(en|ru|de|ar)\/(app|dashboard|admin)/);

  if (isProtected && !req.auth) {
    const locale = pathname.split("/")[1];
    return NextResponse.redirect(new URL(`/${locale}/login`, req.url));
  }

  return intlResponse;
});

export const config = {
  matcher: [
    "/((?!api|_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
  ],
};
```

### 6. Navigation helpers

```typescript
// src/i18n/navigation.ts
import { createNavigation } from "next-intl/navigation";
import { locales, defaultLocale } from "./config";

// Создаём типизированные версии Link, redirect, useRouter, usePathname
export const { Link, redirect, useRouter, usePathname, getPathname } =
  createNavigation({ locales, defaultLocale });
```

### 7. Root layout с locale поддержкой

```typescript
// src/app/[locale]/layout.tsx
import { NextIntlClientProvider } from "next-intl";
import { getMessages, getTranslations } from "next-intl/server";
import { notFound } from "next/navigation";
import { locales, isRtl, type Locale } from "@/i18n/config";
import type { Metadata } from "next";

interface LocaleLayoutProps {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}

// Генерируем SEO metadata для каждой локали
export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "metadata.home" });

  return {
    title: t("title"),
    description: t("description"),
    alternates: {
      // hreflang для SEO
      languages: Object.fromEntries(
        locales.map((l) => [l, `/${l}`])
      ),
    },
  };
}

export function generateStaticParams() {
  return locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({ children, params }: LocaleLayoutProps) {
  const { locale } = await params;

  if (!locales.includes(locale as Locale)) {
    notFound();
  }

  // Загружаем все сообщения для этой локали
  const messages = await getMessages();

  return (
    <html lang={locale} dir={isRtl(locale as Locale) ? "rtl" : "ltr"}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

### 8. Server Component с переводами

```typescript
// src/app/[locale]/dashboard/page.tsx
import { getTranslations } from "next-intl/server";
import { auth } from "@/lib/auth";
import { db } from "@/db";
import type { Metadata } from "next";

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "metadata.dashboard" });
  return { title: t("title"), description: t("description") };
}

export default async function DashboardPage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "dashboard" });
  const session = await auth();

  // Параллельная загрузка данных
  const [projectCount, memberCount] = await Promise.all([
    db.$count(projects, eq(projects.userId, session!.user.id)),
    db.$count(orgMembers, eq(orgMembers.userId, session!.user.id)),
  ]);

  return (
    <div>
      <h1>{t("title")}</h1>
      <p>{t("welcome", { name: session!.user.name })}</p>
      <div>
        {/* ICU plural форматы работают из коробки */}
        <span>{t("stats.projects", { count: projectCount })}</span>
        <span>{t("stats.members", { count: memberCount })}</span>
      </div>
    </div>
  );
}
```

### 9. Client Component с переводами

```typescript
// src/components/auth/login-form.tsx
"use client";

import { useTranslations } from "next-intl";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

// Zod схема с переводами — создаётся внутри компонента чтобы иметь доступ к t()
function useLoginSchema() {
  const t = useTranslations("auth.errors");
  return z.object({
    email: z.string().email({ message: t("emailRequired") }),
    password: z.string().min(8, { message: t("passwordTooShort", { min: 8 }) }),
  });
}

export function LoginForm() {
  const t = useTranslations("auth");
  const loginSchema = useLoginSchema();

  type LoginFormData = z.infer<typeof loginSchema>;

  const form = useForm<LoginFormData>({
    resolver: zodResolver(loginSchema),
  });

  return (
    <form onSubmit={form.handleSubmit(async (data) => {
      // signIn action
    })}>
      <div>
        <Label htmlFor="email">{t("email")}</Label>
        <Input id="email" type="email" {...form.register("email")} />
        {form.formState.errors.email && (
          <p className="text-sm text-red-500">
            {form.formState.errors.email.message}
          </p>
        )}
      </div>
      <div>
        <Label htmlFor="password">{t("password")}</Label>
        <Input id="password" type="password" {...form.register("password")} />
      </div>
      <Button type="submit" className="w-full">
        {t("signIn")}
      </Button>
    </form>
  );
}
```

### 10. Locale Switcher

```typescript
// src/components/locale-switcher.tsx
"use client";

import { useLocale } from "next-intl";
import { useRouter, usePathname } from "@/i18n/navigation";
import { locales, localeNames } from "@/i18n/config";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";

export function LocaleSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  function handleChange(newLocale: string) {
    // useRouter из next-intl автоматически обновляет locale prefix в URL
    router.replace(pathname, { locale: newLocale });
  }

  return (
    <Select value={locale} onValueChange={handleChange}>
      <SelectTrigger className="w-32">
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        {locales.map((l) => (
          <SelectItem key={l} value={l}>
            {localeNames[l]}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  );
}
```

### 11. RTL поддержка с Tailwind

```typescript
// tailwind.config.ts — RTL поддержка через CSS logical properties
// В Tailwind v4 используем CSS-first конфиг

// В CSS файле:
// @import "tailwindcss";
// Используй logical properties: ms-*, me-*, ps-*, pe-* вместо ml-*, mr-*, pl-*, pr-*
// ms = margin-inline-start, me = margin-inline-end

// src/components/ui/rtl-example.tsx
"use client";

import { useLocale } from "next-intl";
import { isRtl } from "@/i18n/config";
import type { Locale } from "@/i18n/config";
import { cn } from "@/lib/utils";

export function Sidebar({ children }: { children: React.ReactNode }) {
  const locale = useLocale();
  const rtl = isRtl(locale as Locale);

  return (
    <aside
      className={cn(
        "fixed top-0 h-full w-64 border-e bg-background", // border-e = border-inline-end (RTL aware)
        rtl ? "right-0" : "left-0"
      )}
    >
      {children}
    </aside>
  );
}
```

### 12. SEO hreflang в Metadata API

```typescript
// src/app/[locale]/blog/[slug]/page.tsx
import type { Metadata } from "next";
import { locales } from "@/i18n/config";

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string; slug: string }>;
}): Promise<Metadata> {
  const { locale, slug } = await params;

  return {
    // hreflang alternate links
    alternates: {
      canonical: `https://myapp.com/${locale}/blog/${slug}`,
      languages: Object.fromEntries(
        locales.map((l) => [l, `https://myapp.com/${l}/blog/${slug}`])
      ),
    },
    openGraph: {
      locale,
      alternateLocale: locales.filter((l) => l !== locale),
    },
  };
}
```

## Антипаттерн

```typescript
// ПЛОХО: Хардкод строк в компоненте
function BadButton() {
  return <button>Submit</button>; // Не переведётся никогда
}

// ПЛОХО: getTranslations в Client Component
// getTranslations — только для Server Components!
"use client";
async function BadClientComponent() {
  const t = await getTranslations("common"); // ← ошибка в runtime
  return <div>{t("save")}</div>;
}

// ПЛОХО: Использование next/link вместо next-intl Link
import Link from "next/link"; // ← теряет locale prefix
function BadNav() {
  return <Link href="/dashboard">Dashboard</Link>; // Переходит на /dashboard, не /en/dashboard
}

// ПРАВИЛЬНО:
import { Link } from "@/i18n/navigation"; // ← сохраняет локаль
function GoodNav() {
  return <Link href="/dashboard">Dashboard</Link>; // → /en/dashboard автоматически
}

// ПЛОХО: locale из URL params в Server Action
export async function serverAction(formData: FormData) {
  "use server";
  const locale = formData.get("locale"); // Не нужно! Используй getLocale() из next-intl/server
  const t = await getTranslations({ locale: locale as string, namespace: "..." });
}
// ПРАВИЛЬНО:
import { getLocale } from "next-intl/server";
export async function serverAction() {
  "use server";
  const locale = await getLocale(); // Читает из request context
}
```

## Связанные документы

- `knowledge/custom/09-seo/` — SEO с i18n, canonical URLs, hreflang
- `knowledge/custom/08-accessibility/` — a11y в RTL языках
- `knowledge/custom/02-patterns/error-handling.md` — локализованные сообщения об ошибках
