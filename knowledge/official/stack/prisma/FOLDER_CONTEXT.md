# Folder Context: `knowledge/official/stack/prisma`

## Purpose

Prisma documentation snapshot covering ORM usage, guides, management APIs, and mixed-version material.

## Structure

- 706 markdown files.
- Root aggregate: `prisma.md`.
- Split files use path-encoded names such as `docs__guides__authentication__authjs__nextjs.md`.
- Source contains mixed version coverage and heterogeneous topic clusters.
- Only the root and a small metadata subset are normalized.

## AI Notes

- Use when Prisma-specific behavior is needed, but remember this repository’s preferred ORM is documented elsewhere in `knowledge/custom`.
- Path names are important retrieval signals.
