# Folder Context: `knowledge/official/devops/vercel`

## Purpose

Normalized Vercel documentation snapshot for deployment, platform, and operational topics.

## Structure

- 680 markdown files.
- Root aggregate: `vercel.md`.
- Split files: numeric names such as `0001-account-management.md`.
- Rich frontmatter includes `id`, `title`, `description`, `category`, `subcategory`, `type`, `source`, `tags`, `related`, and `last_updated`.

## AI Notes

- One of the cleanest sources in the repository for retrieval.
- Prefer split files for precision and `vercel.md` for source-wide overview.
- Good companion source for `knowledge/custom/10-devops`.
