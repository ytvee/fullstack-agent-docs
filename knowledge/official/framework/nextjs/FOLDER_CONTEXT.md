# Folder Context: `knowledge/official/framework/nextjs`

## Purpose

Next.js documentation snapshot centered on App Router-era usage, but still containing broader framework material.

## Structure

- 425 markdown files.
- Root aggregate: `nextjs.md`.
- Split files mostly use numeric prefixes such as `0001_getting-started.md`.
- Many split files include `title`, `description`, `url`, and `version`; the root file also carries source-level metadata.

## AI Notes

- Primary upstream source for routing, rendering, metadata, caching, and deployment behavior.
- Prefer split files for retrieval and `nextjs.md` for source-wide context.
- Pair with `knowledge/custom/01-architecture`, `02-patterns`, `07-performance`, and `09-seo`.
