# Folder Context: `knowledge/official/testing/vitest`

## Purpose

Vitest documentation snapshot for guides, config, browser mode, APIs, and advanced testing features.

## Structure

- 180 markdown files.
- Main entrypoint: `guide.md` rather than `vitest.md`.
- Split files use thematic prefixes such as `guide__`, `config__`, `api__`, and `api__browser__`.
- Files typically include lightweight frontmatter with a `url` key.

## AI Notes

- One of the cleanest split sources in the repository.
- Use prefix families to navigate: `guide__*` for concepts, `config__*` for configuration, `api__*` for APIs.
