interface:
    display_name: 'Frontend Zod Schema'
    short_description: 'Boundary validation patterns with Zod'
    default_prompt: 'Use $frontend-zod-schema when validating boundary input.'
policy:
    allow_implicit_invocation: true
