# Component Patterns

- Separate orchestration concerns from presentational concerns when that makes
  the code easier to reason about.
- Keep props explicit; avoid hidden coupling through module state or magic imports.
- Prefer composition over wrapper pyramids.
- Extract helpers or child components only when readability or reuse improves.
